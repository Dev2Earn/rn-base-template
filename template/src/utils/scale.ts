import { scale as _scale, verticalScale as _verticalScale } from 'react-native-size-matters';


export const scale = (size: number) => {
  return _scale(size);
};
export const verticalScale = (size: number) =>
{
  return _verticalScale(size);
};
export const font = (size: number) =>
{
  return _scale(size);
};